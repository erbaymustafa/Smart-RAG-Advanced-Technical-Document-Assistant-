version: '3.8'

services:
  web-app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OLLAMA_HOST=http://host.docker.internal:11434
    volumes:
      - ./feedback_logs.json:/app/feedback_logs.json